# 技术测试指标与对比清单（发布口径：no-ORT）

更新时间：`2026-03-05`

## 1. 指标定义

- `TTFT p50 (ms)`：首 token 延迟（流式口径）。
- `TPS mean`：平均吞吐。
- `hard_gate`：
  - `status=success`
  - `samples>0`
  - `eligible=true`
  - 无 fallback blocker
- `strict_gate`：
  - `ttft_improve_pct >= 15`
  - `tps_ratio >= 0.95`

## 2. 对比对象

- 基线：`N_B0`
- 候选：Deep12 + 复验候选
- 发布目标：三轨 `llama + mlc + torch_rocm` 均通过 hard + strict

## 3. 当前结论（stage_b_recheck）

- 三轨均满足 hard gate。
- 三轨均满足 strict gate。
- 关键门禁文件：`reports/perf_timeline/non_ort_release_gate.json`（`pass=true`）。

## 4. 证据文件清单

- `reports/perf_timeline/non_ort_tuning_summary.csv`
- `reports/perf_timeline/non_ort_tuning_report.md`
- `reports/perf_timeline/non_ort_release_gate.json`
- `reports/perf_timeline/release_readiness.json`
- `docs/resume_release_v0_1/three_track_before_after.csv`
- `docs/resume_release_v0_1/three_track_before_after.md`

## 5. RGP 与 MLflow

RGP：
- 采集清单：`reports/perf_timeline/rgp_capture_manifest.csv`
- 原始目录：`reports/rgp_raw/`
- 聚合命令：

```powershell
python scripts/collect_rgp_metrics.py --input-root reports/rgp_raw --out-root reports/perf_timeline
```

MLflow：
- 证据文件：`reports/perf_timeline/mlflow_evidence.json`
- 目标状态：`mlflow_used=true`

## 6. 发布前检查

```powershell
python -m ruff check harness scripts tests
pytest -q
python scripts/check_release_readiness.py --help
python scripts/verify_release_bundle.py --release-root <release_root>
```
